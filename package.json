{
  "name": "esriea-facility-token-server",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side ArcGIS token minting + addFeatures proxy for the Create Facility app. Keeps portal credentials off the browser.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "16.3.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
