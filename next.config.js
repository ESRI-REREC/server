/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This project intentionally uses the Pages Router (pages/). All server logic
  // lives in pages/api/*; the single page is documentation for those routes.
};

module.exports = nextConfig;
